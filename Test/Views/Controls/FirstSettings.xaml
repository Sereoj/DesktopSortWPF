<UserControl
    x:Class="Test.Views.Controls.FirstSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:CC="clr-namespace:Test.Resources.Localization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Test.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:FirstSettingsViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="373"
    d:DesignWidth="684"
    lex:LocalizeDictionary.IncludeInvariantCulture="False"
    lex:ResxLocalizationProvider.DefaultAssembly="Test"
    lex:ResxLocalizationProvider.DefaultDictionary="Localization"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24" />
            <RowDefinition />
            <RowDefinition Height="90" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="10,5,0,0"
            FontSize="14"
            Style="{DynamicResource TextBlockTitle}"
            Text="{lex:Loc {Binding Name}}" />
        <Grid x:Name="GridCheckbox" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <CheckBox
                Name="CheckFormat1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat1}"
                Content="{lex:Loc FirstSettingsCheckBox1}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat2"
                Grid.Row="1"
                Grid.Column="0"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat2}"
                Content="{lex:Loc FirstSettingsCheckBox2}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat3"
                Grid.Row="2"
                Grid.Column="0"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat3}"
                Content="{lex:Loc FirstSettingsCheckBox3}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat4"
                Grid.Row="3"
                Grid.Column="0"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat4}"
                Content="{lex:Loc FirstSettingsCheckBox4}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat5"
                Grid.Row="4"
                Grid.Column="0"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat5}"
                Content="{lex:Loc FirstSettingsCheckBox5}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat6"
                Grid.Row="5"
                Grid.Column="0"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat6}"
                Content="{lex:Loc FirstSettingsCheckBox6}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat7"
                Grid.Row="6"
                Grid.Column="0"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat7}"
                Content="{lex:Loc FirstSettingsCheckBox7}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat8"
                Grid.Row="0"
                Grid.Column="1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat8}"
                Content="{lex:Loc FirstSettingsCheckBox8}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat9"
                Grid.Row="1"
                Grid.Column="1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat9}"
                Content="{lex:Loc FirstSettingsCheckBox9}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat10"
                Grid.Row="2"
                Grid.Column="1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat10}"
                Content="{lex:Loc FirstSettingsCheckBox10}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat11"
                Grid.Row="3"
                Grid.Column="1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat11}"
                Content="{lex:Loc FirstSettingsCheckBox11}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat12"
                Grid.Row="4"
                Grid.Column="1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat12}"
                Content="{lex:Loc FirstSettingsCheckBox12}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat13"
                Grid.Row="5"
                Grid.Column="1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat13}"
                Content="{lex:Loc FirstSettingsCheckBox13}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat14"
                Grid.Row="6"
                Grid.Column="1"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat14}"
                Content="{lex:Loc FirstSettingsCheckBox14}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat15"
                Grid.Row="0"
                Grid.Column="2"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat15}"
                Content="{lex:Loc FirstSettingsCheckBox15}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat16"
                Grid.Row="1"
                Grid.Column="2"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat16}"
                Content="{lex:Loc FirstSettingsCheckBox16}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat17"
                Grid.Row="2"
                Grid.Column="2"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat17}"
                Content="{lex:Loc FirstSettingsCheckBox17}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat18"
                Grid.Row="3"
                Grid.Column="2"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat18}"
                Content="{lex:Loc FirstSettingsCheckBox18}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat19"
                Grid.Row="4"
                Grid.Column="2"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat19}"
                Content="{lex:Loc FirstSettingsCheckBox19}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat20"
                Grid.Row="5"
                Grid.Column="2"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat20}"
                Content="{lex:Loc FirstSettingsCheckBox20}"
                Style="{DynamicResource CheckBoxStyle}" />
            <CheckBox
                Name="CheckFormat21"
                Grid.Row="6"
                Grid.Column="2"
                Command="{Binding UpdateCheckBox}"
                CommandParameter="{Binding ElementName=CheckFormat21}"
                Content="{lex:Loc FirstSettingsCheckBox21}"
                Style="{DynamicResource CheckBoxStyle}" />

            <Grid
                Grid.RowSpan="7"
                Grid.Column="3"
                Margin="10,0"
                Opacity="0.8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    Margin="5,5,0,0"
                    FontSize="13"
                    Foreground="{DynamicResource ForegroundTextD}"
                    Style="{DynamicResource TextBlockLeft}"
                    Text="{lex:Loc FirstSettingsIconText}" />
                <Button
                    Margin="0,0,5,0"
                    HorizontalAlignment="Right"
                    Command="{Binding ButtonIconChanger}"
                    Content="&#xE931;"
                    Style="{DynamicResource ButtonIconsSettings}" />
                <CheckBox
                    Grid.Row="1"
                    Margin="0"
                    Content="{lex:Loc FirstSettingsOldFilesText}"
                    IsChecked="{Binding OnlyOldFiles}"
                    Style="{StaticResource CheckBoxStyle}" />
                <CheckBox
                    Grid.Row="2"
                    Margin="0"
                    Content="{lex:Loc FirstSettingsNewFilesText}"
                    IsChecked="{Binding OnlyNewFiles}"
                    Style="{StaticResource CheckBoxStyle}" />
                <CheckBox
                    Grid.Row="3"
                    Margin="0"
                    Content="Только повторяющие"
                    IsChecked="{Binding OnlyDuplicateFiles}"
                    Style="{StaticResource CheckBoxStyle}"
                    Visibility="Hidden" />
            </Grid>

        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Margin="10,-30,0,0"
                Foreground="{DynamicResource ForegroundTextD}"
                Style="{DynamicResource TextBlockLeft}"
                Text="{lex:Loc FirstSettingsCatalogText}" />
            <TextBox
                Name="TextBoxDirectory"
                Grid.Column="0"
                Margin="5,35,5,0"
                Style="{DynamicResource TextBoxStyle}"
                Text="{Binding UpdateTextDirectory, Mode=TwoWay}" />
            <TextBlock
                Grid.Column="1"
                Margin="10,-30,0,0"
                Foreground="{DynamicResource ForegroundTextD}"
                Style="{DynamicResource TextBlockLeft}"
                Text="{lex:Loc FirstSettingsExtensionText}" />
            <TextBox
                Name="TextBoxExtension"
                Grid.Column="1"
                Margin="5,35,5,0"
                Style="{DynamicResource TextBoxStyle}"
                Text="{Binding UpdateTextExtension, Mode=TwoWay}" />

            <Button
                Grid.Column="2"
                Width="115"
                Margin="0,42,14,12"
                HorizontalAlignment="Right"
                Command="{Binding ButtonSaveCommand}"
                Content="{lex:Loc ButtonApply}"
                Style="{DynamicResource ButtonStyleSettings}" />
        </Grid>
    </Grid>
</UserControl>